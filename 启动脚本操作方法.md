cd E:\Tengxun_RAG\WeKnora

# 启动
.\scripts\local-dev.ps1 start

# 查看状态
.\scripts\local-dev.ps1 status

# 停止
.\scripts\local-dev.ps1 stop

# 重启
.\scripts\local-dev.ps1 restart

# 修改 Go 代码后重新编译并启动
.\scripts\local-dev.ps1 restart -Rebuild

# 单独编译后端
.\scripts\local-dev.ps1 build