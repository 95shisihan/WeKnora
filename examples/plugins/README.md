# 框架协议示例

此目录仅保留 `mock-web-search`、`mock-model-provider`、`plain-text-parser`、`memory-retrieval` 四个最小示例，用于展示协议和框架回归测试。

本地目录、飞书、腾讯文档、受控 HTTP 和专有模型等具体插件已迁入独立仓库，见[外部插件目录](../../plugin/EXTERNAL-PLUGINS.md)。管理员安装的是独立仓库构建的 ZIP，宿主运行不依赖这些插件源码。
