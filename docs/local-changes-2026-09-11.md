# 本地改动整理记录（2026-09-11）

本次将此前尚未提交的开发成果纳入 Git，按本地产物忽略、插件功能、本地开发修复分组记录，未推送远端。

## 保留的改动

- 管理员编译 ZIP 安装、插件管理页面、兼容升级和默认数据源名称迁移。
- Windows 原生禁网进程、stdio gRPC、受控 HTTP 及 Go/Python SDK。
- 本地目录检查点恢复、健康状态判断、同步期间停用保护。
- 飞书公开链接、腾讯文档导入、禁网对照插件和可独立使用的开发模板。
- 配套协议、回归测试、CI、安装和升级文档。
- Windows 后台前端启动修复、知识库读取一次重试、网络错误诊断信息。

## 清理内容

- 忽略 `/artifacts/`：编译程序、ZIP、运行库副本、旧版备份和本地验收输出。
- 忽略 `/data/plugins/`：本机安装的插件及其配置。文件保留在本机，当前服务继续使用；未删除知识库或安装包。
- 去掉启动脚本强制覆盖 `WEKNORA_PLUGIN_DIRS` 的临时处理，保留用户显式设置的外部发现目录；安装目录只在未配置时采用默认值。
- 清除新增文件末尾多余空行，格式化一处新增 Go 测试。
- 更新“尚无插件管理页面”和“不能同 ID 升级”等过时说明。
- 源码未发现可仅凭“与插件无关”就应丢弃的已授权功能；前端读取重试和后台启动修复单独记录。

## 本轮验证

- `plugin/test-windows.ps1` 通过：插件核心、协议、SDK、示例、真实 gRPC → 宿主 SQLite 增量测试和前端类型检查。
- 插件全包、数据源包和完整 handler 包测试通过，启用 `sqlite_fts5`；Python stdio 互操作测试实际执行。
- Vue 类型检查通过，设置访问权限、多语言、读取重试共 17 项测试通过。
- Python HTTP SDK 2 项、飞书模板及公开链接插件 28 项测试通过。
- PowerShell 启动脚本与 Node 前端启动器语法检查通过。
- 在沙箱外验证 Windows 原生禁网测试通过；因批量测试输出停滞，停止该批进程后直接运行已构建的 `TestNativeDirectoryStdioIncremental`，健康检查、配置、全量、单文件变化和未变化同步通过。未将批量运行记为全部成功，也未宣称 WFP 拒绝日志验收通过。
- 暂存区差异检查通过，未纳入 EXE、DLL、ZIP、运行数据或本地环境配置。

最初直接调用 Go 测试遇到 Windows GCC 内部链接器的运行库查找错误。显式通过 `-extldflags=-B<所选工具链的 bin 目录>/` 指定链接器后通过，修复已加入 Windows 验收脚本，不依赖写死本机绝对路径。

本轮没有重新部署当前服务，也没有重跑真实飞书/腾讯文档的外网导入。历史业务验收及 Linux AppArmor、Windows WFP 日志的边界仍见 [验收矩阵](../plugin/ACCEPTANCE.md)。本地 `artifacts/` 中的日志不随源码提交，可由验收脚本重新生成。
